name: test-and-lint

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Install
      run: npm ci

    - name: Test
      run: npx jest --coverage

    - name: Lint
      run: npx eslint .
